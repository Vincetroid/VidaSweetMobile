import { StyleSheet } from 'react-native';

export const gStyles = StyleSheet.create({
  gralContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  gralMargin: {
    margin: 16,
  },
});

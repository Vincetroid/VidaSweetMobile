import { StyleSheet } from 'react-native';
import { themeStyles } from '@/global-styles';

export const styles = StyleSheet.create({
  defaultFixedBottomStyles: {
    backgroundColor: themeStyles.black,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

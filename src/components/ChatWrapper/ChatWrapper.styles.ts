import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  chatViewWrapper: {
    position: 'absolute',
    top: 60,
    right: 10,
  },
  icon: {
    color: '#25D366',
    alignSelf: 'center',
  },
});
